[pytest]
testpaths = tests
python_files = test_*.py
# A broken global pytest-qt/PyQt6 install can crash collection on some machines;
# block it so this suite stays self-contained.
addopts = -q -p no:pytest-qt
filterwarnings =
    ignore::DeprecationWarning
